import { StyledHeader } from "../styles/Header.styled"
import Navbar from "./Navbar"

const Header = () => {
  return (
    <StyledHeader>
        <Navbar/>
    </StyledHeader>
  )
}

export default Header